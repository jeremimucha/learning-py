import setuptools
import os

# _scriptdir = os.path.abspath(os.path.dirname(__file__))

# with open(os.path.join(_scriptdir, 'requirements.txt'), 'r') as fh:
#     requires = fh.read()


setuptools.setup(
    install_requires=['requests']
)
